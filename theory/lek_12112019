#hash - обращение к браузеру - не меняет url, не доходит до сервера
?name=elias - Query string - меняет url
:234432 - порты, считай другой сервер

URL 
Так же информацию можно передавать с помощью заголовков. Он не видны в адресной строке.
Метод
Тело сообщения
- данные, по которым сервер может получать данные

curl [url]
curl [url] -XGET
curl [url] -XPOST

2 байта - символы переноса строки - код 10

http HyperText Transfer Protocol
\r - код 13

curl [url] -Р "Content-Type: text/html" - послать заголовок 

postman - для посылки сообщений серверу

... rest или spread

MIME-type - помогает понять программе, как отображать полученные данные

байт = это столько бит, сколько надо
октет - 8 двоичных разрядов, наиболее распространенный вид байта
